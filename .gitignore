node_modules
.pnpm-store
*.log
.DS_Store
.env
.env.local
dist
.cache
.tmp

# Keep the forked react-scan build artefacts in-repo so the MCP server
# works on a fresh clone without users having to run a build.
!packages/scan/dist/
